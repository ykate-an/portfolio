import { ko, en } from "../../../locale";
import { interests } from "../../../database";
import { Badge } from "theme-ui";

## Interests

{Object.keys(interests).map((item)=>{
return (

<Badge backgroundColor="icon_pink">{en[`${interests[item]}`]}</Badge>)

})
}
