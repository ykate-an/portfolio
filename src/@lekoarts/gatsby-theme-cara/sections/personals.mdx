import { ko, en } from "../../../locale/index.mjs";
import { projects } from "../../../database.mjs";
import { Text, Paragraph } from "theme-ui";

export const bgColors = [
  "linear-gradient(to right, #D585FF 0%, #00FFEE 100%)",
  "linear-gradient(to right, rgba(244,132,132,1) 0%, rgba(238,174,202,1) 30%, rgba(236,245,255,1) 100%)",
  "linear-gradient(to right, #D4145A 0%, #FBB03B 100%)",
  "linear-gradient(to right, #662D8C 0%, #ED1E79 100%)",
];

export const personals = projects.map((project, idx) => {
  return {
    ...project,
    bg: bgColors[idx],
  };
});

{<Text>Personal projects</Text>}

{Object.keys(personals).map((key) => {

return (

<ProjectCard
title={en[`${personals[key].title}`]}
link={`/${personals[key].path}`}
bg={personals[key].bg}
key={key}>
{<Paragraph>{en[`${personals[key].role}`]}</Paragraph>}
{<Paragraph pt={1}>{en[`${personals[key].period}`]}</Paragraph>}
{<Paragraph pt={3}>{en[`${personals[key].description}`]}</Paragraph>}

</ProjectCard>)

})}
