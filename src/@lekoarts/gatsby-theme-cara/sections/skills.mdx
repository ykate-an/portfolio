import { ko, en } from "../../../locale/index.mjs";
import { skills } from "../../../database.mjs";
import { Badge } from "theme-ui";

## Skills

{Object.keys(skills).map((field)=>{
return ( Object.keys(field).map((item)=>{
return (

<Badge backgroundColor="icon_purple">{en[`${skills[field][item]}`]}</Badge>)

        }))

})
}
