import { ko, en } from "../../../locale/index.mjs";
import { skills } from "../../../database.mjs";
import { Badge, Text } from "theme-ui";

## {<Text>Skills</Text>}

#

#

#### Backend

{Object.keys(skills.backend).map((item)=>{
return(

<Badge backgroundColor="icon_purple" m={1} p={2}>
  {en[`${skills.backend[item]}`]}
</Badge>
)}) }

#

#### Frontend

{Object.keys(skills.frontend).map((item)=>{
return(

<Badge backgroundColor="icon_purple" m={1} p={2}>
  {en[`${skills.frontend[item]}`]}
</Badge>
)}) }

#

#### Others

{Object.keys(skills.others).map((item)=>{
return(

<Badge backgroundColor="icon_purple" m={1} p={2}>
  {en[`${skills.others[item]}`]}
</Badge>
)}) }
